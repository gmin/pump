server:
  port: ":8080"

database:
  host: "localhost"
  port: "5432"
  user: "postgres"
  password: "postgres"
  dbname: "pump_token"

jwt:
  secret: "your-secret-key"

solana:
  rpc_url: "https://api.mainnet-beta.solana.com"
  program_id: ""  # 合约程序ID
  admin_wallet: ""  # 管理员钱包地址
  fee_rate: 0.06  # 手续费率(6%)
  min_fee: 6  # 最小手续费(6 SOL) 